import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function BoardCardPlaceholder() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Board foundation</CardTitle>
        <CardDescription>Ready for list rendering, drag-and-drop lanes, and realtime sync.</CardDescription>
      </CardHeader>
    </Card>
  );
}
